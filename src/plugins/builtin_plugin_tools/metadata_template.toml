[plugin]
enabled = true

[plugin.about]
name = "Plugin Tools"
version = "0.0.1"
description = "Provides functionality to manage plugins."

[plugin.commands]
disable_commands = []
disable_parameters = []
default_permission_groups = ["guest", "regular", "admin"]
