[plugin]
enabled = false

[plugin.about]
name = "Sound Board"
version = "0.0.1"
description = "Provides sound board functionality to Mumimo."

[plugin.commands]
disable_commands = []
disable_parameters = []
default_permission_groups = ["guest", "regular", "admin"]
